on:
  workflow_dispatch:

jobs:
  test-ansible-connections:
    runs-on: [ self-hosted, linux ]

    steps:
    
      - name: Test connections with ping module
        run: |
          ansible all --list-hosts -i /home/rochelle/ansible/inventory.yaml
          ansible all -m ping -i /home/rochelle/ansible/inventory.yaml
