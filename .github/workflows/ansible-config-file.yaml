on:
  workflow_dispatch:

jobs:
  generate-ansible-cfg:
    runs-on: [ ansible-master ]

    steps:


      - name: Generate ansible.cfg
        run: |
          sudo -u rochelle bash -c 'cat > /home/rochelle/ansible/ansible.cfg' << 'EOF'
          [defaults]
          inventory      = /home/rochelle/ansible/inventory.yaml
          roles_path    = /home/rochelle/ansible/roles
          host_key_checking = False
          remote_user = rochelle
          private_key_file = /home/rochelle/.ssh/id_rsa
          log_path = /home/rochelle/ansible/ansible.log
          forks = 5
          gathering = smart
          fact_caching = jsonfile
          fact_caching_connection = /home/rochelle/ansible/facts_cache
          

          [privilege_escalation]
          become = True
          become_method = sudo
          become_user = root
          become_ask_pass = False

          [ssh_connection]
          ssh_args = -o ControlMaster=auto -o ControlPersist=60s -o ConnectTimeout=10
          control_path = /home/rochelle/.ssh/id_rsa-%%r@%%h:%%p
          EOF
          

      - name: Set proper permissions
        run: |
          sudo chown rochelle:rochelle /home/rochelle/ansible/ansible.cfg
          sudo chmod 644 /home/rochelle/ansible/ansible.cfg

      - name: Verify configuration
        run: |
          echo "Ansible configuration:"
          sudo -u rochelle ls -la /home/rochelle/ansible/ansible.cfg
          echo -e "\nContents:"
          sudo -u rochelle cat /home/rochelle/ansible/ansible.cfg
